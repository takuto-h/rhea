dynamic_wind(
  ^(){ p(:a); },
  ^(){ p(:b); },
  ^(){ p(:c); }
);
def cont = 0;
^(){
  p(:d);
  p(callcc(^(c){ cont = c; :e; }));
  p(:f);
}();
dynamic_wind(
  ^(){ p(:g); },
  ^(){ p(:h); cont(:i); p(:j); },
  ^(){ p(:k); }
);
dynamic_wind(
  ^(){ p(:l); },
  ^(){ p(:m); p(callcc(^(c){ cont = c; :n; })); p(:o); },
  ^(){ p(:p); }
);
cont(:q);
